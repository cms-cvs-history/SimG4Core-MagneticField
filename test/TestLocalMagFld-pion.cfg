process PROD  = 
{

   service = MessageLogger
   {
      untracked vstring destinations = {"cout"}      
      #untracked vstring categories = { "FwkJob" }             
      untracked PSet cout = 
      { 
         untracked PSet default = { untracked int32 limit = 0 }
	 #untracked PSet FwkJob  = { untracked int32 limit = -1 }
      }
   }
   
   service = Timing {}

   service = RandomNumberGeneratorService
   {
      untracked uint32 sourceSeed = 98765
      PSet moduleSeeds =
      {
         untracked uint32 VtxSmeared = 12345
         untracked uint32 g4SimHits = 9876
      }
   }
   
   ###include "SimGeneral/HepPDTESSource/data/pdt.cfi"
   include "SimGeneral/HepPDTESSource/data/pythiapdt.cfi"
      
   # flat random E-gun, single pion
   #
   # to run it along with CMS detector simulation
   # (OscarProducer) make sure to select QGSP physics
   # list, instead DummyPhysics ("Dummy" has only EM 
   # process and wont know to model interactions of
   # hadrons with matter)
   #
   source = FlatRandomEGunSource
   {
      untracked int32 maxEvents = 1
      untracked PSet PGunParameters =
      {
         # you can request more than 1 particle
         #untracked vint32  PartID = {211,11}
         untracked vint32 PartID = {211}
         untracked double MinEta = -1.0
         untracked double MaxEta =  1.0
         untracked double MinPhi = -3.14159265358979323846 # in radians
         untracked double MaxPhi =  3.14159265358979323846
         untracked double MinE   =  9.99
         untracked double MaxE   = 10.01
      }
      untracked int32 Verbosity = 0 # set to 1 (or greater)  for printouts
   }
   
   include "IOMC/EventVertexGenerators/data/VtxSmearedGauss.cfi"

   #Geometry
   #   
   include "Geometry/CMSCommonData/data/cmsSimIdealGeometryXML.cfi"
   es_module = TrackerGeometricDetESModule {}

   # Magnetic Field
   #
   include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"
   
   # Geant4-based CMS Det.Simulation
   #
   # the 'replace' option below show how to override the default
   # setting of mag.field key (you turn it off)
   #
   include "SimG4Core/Application/data/g4SimHits.cfi"
   replace g4SimHits.MagneticField =
   {
      double delta = 1.
      untracked bool Verbosity = true
      bool UseLocalMagFieldManager = false
      untracked PSet ConfLMFM =
      {
         vstring ListOfVolumes = { "CALO" }
	 PSet CALO =
	 {
	    string Type = "CMSIMField"
	    string Stepper = "G4HelixExplicitEuler" # can be vstring but that'll require
	                                             # more work on the code/infrastructure 
	    PSet G4HelixExplicitEuler = 
	    {
	       double MinStep = 1. # in mm
	       double DeltaChord = 1. # in mm
	       double DeltaOneStep = 1. # in mm
	       double DeltaIntersection = 0.25 # in mm
	       #double DeltaIntersectionAndOneStep = -1.
	       #int32  MaximumLoopCounts = 1000
	       #double MinimumEpsilonStep = 0.00001  # in mm
	       #double MaximumEpsilonStep = 0.01 # in mm
	    }
	 }
      }
   }
   		  
   path p1 = { VtxSmeared, g4SimHits }
   
}		
